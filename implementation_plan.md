# План реализации НМОУ Вектор (Система управления бизнесом)

## Цель продукта

НМОУ Вектор — платформа для мониторинга ключевых показателей организации и их динамики во времени. Каждый экземпляр сайта обслуживает ровно одну организацию и до 50 одновременных пользователей; для другой организации разворачивается отдельный экземпляр со своей БД.

Платформа должна отделять администратора с платформенными правами от единого рабочего пространства организации. Любой неадминистративный аккаунт получает через должность только нужные рабочие вкладки и может переходить между ними в общей навигации. Система должна проводить данные через подтверждение перед использованием в аналитике, сохранять историю исправлений и готовиться к интеграциям с 1C, Excel, Google Sheets, Email и MAX.

## Текущее состояние после очистки

- Тестовая визуальная страница удалена.
- Тестовые Excel-данные и агрегаты удалены из проекта.
- Проект оставлен как чистый Vite + React + TypeScript + CSS каркас.
- Документация в `docs/` держит короткий локальный runbook `docs/local-server-run.md` и отдельную инструкцию внешнего Google Sheets источника `docs/google-sheets-source.md`.
- Добавлен первый TypeScript-контракт серверного ответа для типов аккаунта и прав доступа в `src/contracts/accounts.ts`.
- Собран минимальный app shell: left rail, кнопка выхода и рабочие поверхности без placeholder-плашек после входа.
- Диспетчерская система перенесена в backend-контур как 6 активных server-owned форм и единый журнал регистраций.
- Реальная разработка должна начинаться от этого плана, без переноса старого визуального прототипа.

## Статус реализации

- [x] Старый визуальный прототип удалён.
- [x] Короткий runbook локального запуска и локального Docker хранится в `docs/local-server-run.md`; он дополнительно закрепляет проверку единственного Vite-процесса и диагностику устаревшей вкладки/HMR через реально отданные CSS/JS и временный чистый origin. Внешний Google Sheets источник справочников описан отдельно в `docs/google-sheets-source.md`.
- [x] Базовый контракт аккаунтов создан в `src/contracts/accounts.ts`.
- [x] Первый визуальный app shell собран без клиентских бизнес-данных.
- [x] Все компактные модальные окна закрываются нажатием на фон вне окна; нажатия внутри не закрывают форму, а во время сохранения или другого выполняющегося изменения применяется тот же запрет закрытия, что и для кнопки отмены. Окна создания и просмотра поручений приведены к уже используемому общему поведению административных окон.
- [x] Мобильные окна поручений больше не выходят одновременно за верхнюю и нижнюю границы при низком viewport или открытой экранной клавиатуре: их максимальная высота ограничена внутренней областью backdrop после отступов, содержимое прокручивается внутри окна, а верхний и нижний отступы учитывают safe-area телефона.
- [x] Экран входа после обычного открытия и перезагрузки всегда начинается с верхней границы собственной прокручиваемой области: lifecycle `pageshow` временно удерживает верх при запоздалом восстановлении Safari даже после следующего кадра, освобождает защиту сразу после реального пользовательского ввода, а завершение загрузки профиля и карточек доступа повторяет сброс после изменения высоты содержимого. Jino дополнительно запрещает кэширование HTML, чтобы после deploy Safari сразу получил ссылки на актуальные hashed assets.
- [x] Регрессионный тест восстановления экрана входа совместим с Jino Node.js 22: временные JSDOM browser globals устанавливаются и восстанавливаются через property descriptors, включая getter-only глобальный `navigator`; отдельная getter-only регрессия и полный последовательный `test:jino` проходят локально.
- [x] Экран входа и левый rail используют единый брендмарк НМОУ Вектор с тремя сходящимися направлениями и стрелкой; на экране входа иконка отображается без фоновой подложки. Его прозрачные версии подключены как основная иконка, favicon 32×32 и Apple Touch Icon 180×180, а прежний radar-брендмарк и неиспользуемые черновые концепты удалены. Заголовок вкладки зависит от окружения: `Вектор` в production и `Вектор Тест` в test, а email/MAX-уведомления по умолчанию используют префикс `НМОУ Вектор`.
- [x] В левом rail под названием платформы отображается сокращённое имя реально вошедшего пользователя в формате `Фамилия И.`, включая admin preview без подмены личности администратора; после успешного входа один раз показывается короткое самозакрывающееся приветствие с тем же именем. Приветствие, уведомления отправки и подтверждения успешных административных действий используют единый фиксированный вертикальный стек в правом нижнем углу: новые сообщения появляются сверху, выезжают справа, перед исчезновением уезжают обратно, а оставшиеся уведомления плавно занимают освободившееся место. Такие подтверждения не добавляются в поток страницы и не меняют позицию прокрутки; при фоновом обновлении после административного действия уже показанные таблицы сохраняются до прихода новых данных и не схлопывают страницу. Загрузка, валидация и ошибки остаются рядом с соответствующим действием.
- [x] Администратор получил компактную вкладку `Действия пользователей`, защищённую capability `platform.view_audit`; ту же вкладку можно явно включить в настройке любой неадминистративной должности, где отдельная capability `business.view_user_actions` жёстко ограничивает события и список аккаунтов организационным scope. Отчёт фильтруется по отдельной учётной записи и понятным категориям, показывает входы/выходы, отправки форм с server-allowlisted значениями, изменения данных, административные действия и просмотры основных вкладок/форм. Backend хранит append-only события в `user_audit_events`, автора берёт только из серверной сессии, сверяет заявленный просмотр с доступами аккаунта или реальным admin preview, не пишет пароли/session IDs/секреты/произвольный raw payload и всегда ограничивает отчёт последними тремя календарными месяцами без удаления более старых событий или связанных данных. Production-изменение и его обязательное audit-событие фиксируются через общий `DatabaseTransactionRunner` одной транзакцией; сбой аудита откатывает изменение, а внешние уведомления отправляются после commit.
- [x] Добавлена server-owned вкладка `Поручения Совета директоров`: члены Совета директоров создают поручения Генеральному директору по реквизитам заседания, протокола и пункта решения, полному и краткому содержанию, соисполнителям и структурированному сроку. Срок поддерживает варианты каждый день, неделю, месяц, год или один раз и ограничивается датами начала и окончания; дата заседания автоматически подставляется в пустые границы. Для каждого варианта доступа показывается отдельная понятная вводная карточка: общий реестр, постановка, активное исполнение или проверка. Исполнитель получает только наступившие поручения в работе или на доработке, включая просроченные, и не может открыть неактивное поручение напрямую; после передачи на проверку карточка закрывается и поручение исчезает из активного списка. После приёмки повторяющееся поручение переходит к первой дате расписания после дня приёмки в пределах периода без очереди пропущенных повторов. Пользователь с правом приёмки может завершить поручение или вернуть его на доработку. В форме должности секция `Рабочие вкладки` показывает четыре взаимоисключающих варианта: только просмотр; просмотр и создание; исполнение и отправка на проверку; создание, приёмка и возврат на доработку. Backend сохраняет выбранный вариант через server-owned navigation/capabilities независимо от названия должности. Каждый комментарий сохраняется append-only с серверным автором, временем и статусом, а изменение статуса и audit-событие проходят одной транзакцией. Миграция `025_board_assignments` создаёт реестр, историю и защищённые должности проверяющих, а также одной транзакцией импортирует с audit-событиями 11 адресованных Генеральному директору поручений протокола №369 от 10.07.2026; миграция `026_board_assignment_schedules` переводит существующие сроки в структурированные расписания и активные повторы. Пункт 1.1, адресованный рабочей группе, в реестр Генерального директора не включается. Скан протокола хранится вне публичной статики и открывается только через authenticated PDF endpoint как первый дополнительный материал. В формах создания и изменения можно прикрепить до пяти PDF размером до 10 МБ каждый, удалить или вернуть существующий файл до сохранения; backend повторно проверяет PDF и хранит его в защищённой MariaDB/MySQL таблице миграции `029_board_assignment_documents`. Удаление из живой версии мягкое, поэтому документ остаётся доступен в неизменяемом снимке уже выполненного поручения. Нужный вариант доступа Ларикову А.Т., Самсонову Г.Н. и Глушкову Ю.В. назначается после deploy только через штатный admin-flow, чтобы изменение было авторизовано, аудировано и отзывало действующие сессии; буквальная сверка полей формы с Положением остаётся до получения самого текста Положения, которого нет в приложенном протоколе.
- [x] Интерфейс поручений разделён на четыре контекстных сценария вместо универсальной карточки доступа. Наблюдатель получает спокойный read-only реестр с полными фильтрами; постановщик — заметный первый шаг создания и отдельный счётчик реестра; исполнитель — карточки только активных поручений, короткий поиск, обязательный комментарий и одну прямую кнопку `Отправить на проверку` без выбора статуса; проверяющий — отдельную очередь поручений `На проверке`, прямые кнопки `Принять исполнение` и `Вернуть на доработку`, а также полный реестр и создание новых поручений.
- [x] Любой пользователь с правом создания поручений может из живой карточки отредактировать все поля незавершённого поручения с обязательным комментарием к изменению; автор исходной записи не ограничивает доступ. Backend пересчитывает ближайшую текущую дату по новому расписанию без возврата к уже пройденным периодам, сохраняет append-only before/after ревизию и audit-событие в общей транзакции. При каждой приёмке сохраняется отдельный неизменяемый полный снимок состояния выполненного периода до перехода повторяющегося поручения к следующей дате. Вкладка получила переключатель `Текущие поручения` / `История выполненных`; история сортируется по времени приёмки, поддерживает поиск и даты заседания, а карточка снимка показывает все зафиксированные поля и комментарии без действий редактирования. Миграция `027_board_assignment_editing_and_completion_history` добавляет ревизии правок и снимки завершений.
- [x] Roadmap переведён в режим checklist: выполненное, активный пункт и следующий шаг должны обновляться после каждой задачи.
- [x] Серверные контракты шага 1 реализованы для user profile, organization scope, data entry, confirmation и audit summary; модель подразделений удалена миграцией `016_remove_departments`, а multi-business модель — миграцией `017_single_organization_scope`.
- [x] Клиентская fetch-boundary для `access/profile` создана в `src/services/accessProfile.ts` с loading, empty и error states.
- [x] Локальный Vite server endpoint `GET /api/access/profile` подключён и возвращает JSON профиль выбранной dev-сессии или empty state без бизнес-данных.
- [x] Временное окно авторизации добавлено: кнопки создают server-side dev-сессию admin / business owner / worker / dispatcher.
- [x] Все неадминистративные аккаунты используют единое рабочее пространство и видят только вкладки, выданные их должности; администратор остаётся в отдельном platform-контуре с просмотром рабочих аккаунтов и server-gated экраном БД.
- [x] Добавлена remote API boundary для диспетчерских отправок: `POST/GET /api/dispatcher/submissions` через `VITE_SMB_REMOTE_API_URL`.
- [x] Если удалённый сервер не подключён или недоступен, временный dev-вход возвращается к локальным Vite endpoints, а если `/api` хостинга отсутствует — к клиентской тестовой dev-сессии в `sessionStorage`; диспетчерские формы, отправка и live-история переходят в явно помеченный локальный тестовый `localStorage` fallback. Этот режим нужен только для проверки интерфейса и не заменяет backend.
- [x] Backend workspace `server/` создан в этом репозитории: Node.js + TypeScript API, MariaDB/MySQL, миграция `dispatcher_submissions`, endpoints `POST/GET /api/dispatcher/submissions`.
- [x] Локальная MariaDB подключена через `docker-compose.yml`; данные dispatcher submissions хранятся в Docker volume и переживают рестарт контейнера.
- [x] Для Jino выбрана managed MariaDB `j53403317_bot1` с прикладным пользователем `j53403317_robot`; миграции `schema_migrations` и `dispatcher_submissions` применены к этой БД.
- [x] Локальные env-файлы `.env` и `server/.env` подготовлены для запуска backend на `127.0.0.1:3000`.
- [x] Короткая инструкция запуска локального сервера и отдельного локального Docker создана в `docs/local-server-run.md`.
- [x] Подготовлен постоянный Windows server-PC профиль: MariaDB запускается с `restart: unless-stopped`, порт БД опубликован только на `127.0.0.1`, добавлены PowerShell-скрипты запуска и регистрации в Task Scheduler.
- [x] Deployment-договорённость обновлена: frontend и backend код хранятся в `~/domains/smb.aonmou.ru/app` на Jino, собранная статика публикуется в `public_html`; для запуска backend на самом Jino нужен Node.js/web-app runtime, а не только PHP-интерпретатор.
- [x] Backend `DATABASE_URL` поддерживает Jino Unix socket через `socketPath`, чтобы managed MariaDB могла подключаться как локальная `localhost`-БД, если TCP `127.0.0.1:3306` отклоняет пользователя.
- [x] Runtime whitelist capabilities добавлен: клиент отклоняет неизвестные capability из server profile.
- [x] Добавлены тесты `requestAccessProfile`, dev access session, dispatcher remote boundary и server-domain validation через `npm test`.
- [x] Диагностика remote API усилена: при сетевом/CORS-сбое диспетчерской отправки UI показывает проверку `/health`, точный browser origin для `CORS_ORIGIN` и предупреждение про `127.0.0.1` при frontend с другого ПК.
- [x] Remote backend теперь отдаёт временный `access/profile` и `dev/access-session`, а frontend при `VITE_SMB_REMOTE_API_URL` направляет access/dev запросы в backend API вместо `/api` хостинга страницы.
- [x] Vercel frontend origins закреплены в server env example и remote docs: `https://smb-umber.vercel.app` и preview-паттерн `https://smb-*-artemi-z-s-projects.vercel.app`.
- [x] Backend CORS поддерживает hostname-паттерны для Vercel preview, а `access/profile` при network failure показывает `/health` и `CORS_ORIGIN` диагностику.
- [x] Backend CORS разрешает `DELETE` preflight для очистки временной `dev/access-session`, чтобы кнопка выхода работала через remote API boundary.
- [x] В `AGENTS.md` закреплено правило единого frontend/backend-контура: сервер обновляется через Git, поэтому связанные изменения должны попадать в tracked-код, docs и тесты сразу для всего проекта.
- [x] В `AGENTS.md` закреплено правило прямого редактирования `server/.env` как runtime-конфига backend без раскрытия секретов.
- [x] В `AGENTS.md` закреплено предварительное сообщение с краткой оценкой задачи; уровень reasoning сообщается только при доступном MCP `reasoning-agents`, а при отключённом плагине основной агент не выбирает уровень и не создаёт заменяющий агент, чат или процесс.
- [x] Активный app shell получил единую адаптивную навигацию и кнопку выхода: на desktop/горизонтальном интерфейсе левый rail закреплён по высоте viewport и всегда остаётся открытым без кнопки скрытия или кликабельного логотипа; на mobile по умолчанию остаётся верхняя панель с логотипом и кнопкой справа, которая открывает drawer слева поверх страницы. Каждый клик по пункту левого rail, включая уже активный, заново открывает главную страницу выбранной вкладки и сбрасывает её вложенный экран; для `Диспетчерской` также восстанавливаются исходный раздел и период. Мобильное меню закрывается после выбора раздела, внутренней кнопкой закрытия, по затемнению и `Escape`, удерживает клавиатурный фокус внутри, возвращает его кнопке открытия и учитывает `prefers-reduced-motion`.
- [x] Все реальные состояния ожидания frontend-а используют единый минималистичный индикатор загрузки: крупный вариант показывает первичную загрузку страницы или раздела, компактный — загрузку блока и данных формы, встроенный — вход, выход, отправку и административные операции. Индикатор объявляется через `role="status"`, не заменяет уже загруженные таблицы при фоновом обновлении и отключает движение при `prefers-reduced-motion`.
- [x] Интерфейс всех профилей очищен от placeholder-плашек: переключение владельца идёт только через левый rail, `Обзор` сочетает server-owned сводку с начала текущего месяца по инцидентам и лабораторным испытаниям с компактными блоками оборудования, выработки и посетителей из сохранённой dispatcher-истории, `Диспетчерская` показывает только live-данные, worker не видит диспетчерские формы, dispatcher работает с формами и доступной ему общей историей, а admin — только через server capabilities.
- [x] Диспетчерская получила серверный список из 6 активных форм: выработка, оборудование, открытие инцидента, закрытие инцидента, вход посетителя и выход посетителя; газовые формы скрыты из выбора.
- [x] Диспетчер выбирает форму из 4 визуальных групп: производство, оборудование, регистрация/закрытие инцидента и вход/выход посетителя; группы отличаются позицией и цветовым акцентом, затем диспетчер может вернуться к выбору из анкеты, заполняет серверно разрешённые поля и отправляет `formId + payload` в `POST /api/dispatcher/submissions`.
- [x] В каждой открытой диспетчерской форме рядом с её названием доступна кнопка `Посмотреть историю`: она открывает тот же server-gated экран истории, что и у высших руководителей, с разделами `Выработка`, `Оборудование`, `Инциденты`, `Посетители` и общими фильтрами периода. История сразу выбирает раздел текущей формы, а возврат сохраняет введённый черновик формы.
- [x] Добавлена server-owned форма `Выработка` по переданному бумажному бланку: таблицы на всю доступную ширину содержат дату отчёта, четыре одинаковые динамические таблицы формовки, сортировки, неформованной продукции/контейнеров и шамота, три банки с отдельными замерами начала/конца дня и участок грануляции с фракциями `16/30` и `12/18`. В каждой производственной категории можно добавлять до 50 марочных столбцов с поисковым выбором марки и полем `Факт за сутки`; все марки берутся из первого столбца `Наименование` вкладки Google Sheets `Номенклатура`. Любой заполненный факт, включая `0`, требует марки, выбранная марка требует факт, а повторы внутри категории отклоняются frontend-ом и backend-ом. Старые одиночные поля формовки/сортировки продолжают читаться и при повторной отправке преобразуются в динамический формат. Backend перед DB-транзакцией канонизирует марки по актуальной номенклатуре. Дневной план, накопительный план до выбранной даты включительно, месячный факт и отклонение `факт − план` отображаются в каждой из четырёх таблиц одинаковыми read-only колонками и не входят в payload. Для live-расчёта backend исключает факт редактируемого дня и возвращает сохранённый месячный факт предыдущих дней, а форма добавляет сумму текущих марочных фактов, поэтому сохранённый день не удваивается. Те же месячные план/факт, марочные накопления и отклонение используются в руководительской `Диспетчерской`; отправка сохраняется через общий dispatcher submissions контур и фиксируется обязательным аудитом. Выбор поддерживает поиск; отсутствующую марку диспетчер или пользователь ОЦ может добавить через server-gated API в первую свободную ячейку столбца A, начиная с `A2`. Отдельная таблица марок удаляется миграцией `022_google_sheets_production_brands`, а разделы марок исключены из админской `БД`.
- [x] В субботу и воскресенье диспетчер может отправить дневную `Выработку` с заполненной `Сортировкой` и без факта `Формовки`: незавершённая строка Формовки с выбранной маркой очищается и не попадает в payload/БД. Факт без марки остаётся ошибкой; рабочие дни и остальные производственные категории сохраняют обязательную пару `марка + факт`. Правило одинаково действует в HTML-форме, клиентской проверке, локальном test fallback и backend validation.
- [x] Во всех четырёх динамических таблицах закрытый выбор марки и пункты раскрытого списка используют явный интерфейсный шрифт размером `16px`, чтобы нативное меню браузера не уменьшало и не подменяло текст.
- [x] Добавлена отдельная должность и вкладка экономиста `План выработки`: экономист свободно выбирает месяц через переключатели месяца/года со стрелками и открывает любую из четырёх категорий без последовательной блокировки; поле месячного плана принимает положительные целые и дробные значения с точкой или запятой, нормализует запятую в точку и ограничивает дробную часть двумя знаками; активная кнопка категории выделяется отдельной тёмной заливкой и визуально отличается от светлой отметки уже сохранённого плана. Каждая категория получает независимое расписание с пресетами `Все дни` и `Только рабочие` (понедельник–пятница), после чего любую дату можно включить или выключить вручную; категория сохраняется отдельно, а возвращённая сервером составная сводка выбранного месяца сразу обновляется в нижнем блоке. План каждой категории является суммарным по всем её маркам. До предпоследнего дня категории backend ставит целую округлённую вверх долю, на последний — точный остаток с точностью до сотых; отрицательный остаток отклоняется. Доступ защищён capability `business.manage_production_plan`, каждое сохранение добавляет append-only ревизию месячного снимка `production_plan_revisions` и обязательное audit-событие одной транзакцией. Старые ревизии с единым календарём читаются как четыре одинаковых расписания. Форма диспетчера загружает только планы категорий, назначенные на выбранную дату, показывает отсутствие плана отдельно и не отправляет планы в payload.
- [x] Поля дат в диспетчерских формах по умолчанию заполняются сегодняшним днём, поля дата-время — текущими датой и временем, поля месяца показывают форматированный ввод `ММ.ГГГГ` с текущим месяцем и проверкой диапазона `01–12`, а числовые поля принимают целые и дробные значения с точкой или запятой, нормализуя запятую в точку.
- [x] Логика анкет из переданных Apps Script перенесена в серверные правила: оборудование требует хотя бы одно значение отчёта, выводит месяц из даты, не принимает причину простоя без времени простоя больше 0, не принимает время простоя больше 0 без причины простоя, для причины `Резерв` допускает только 8 часов простоя, ограничивает простой за смену 8 часами и требует выработку больше 0 при простое меньше 8 часов; открытие инцидента использует исходные списки типов/критичности и получает серверный номер `INC-год-номер`; закрытие инцидента выбирается только из незакрытых инцидентов, не ограничено днём открытия и повторно не закрывает уже закрытый номер; вход посетителя получает время входа на сервере и запрещён для уже вошедшего посетителя; выход посетителя выбирается только из сегодняшних вошедших и ещё не вышедших.
- [x] Выход посетителя устойчиво связывается с конкретным `visitorEntryId`: клиент не теряет выбранного посетителя даже при устаревшем server form description, а клиентская и серверная обработка корректно закрывают визит при одинаковых timestamp входа/выхода.
- [x] После каждого успешного явного входа в аккаунт с назначенной вкладкой `Форма` диспетчер один раз видит информационное окно с точным числом незакрытых инцидентов из server-owned `openIncidents`. `Продолжить работу` закрывает окно без смены экрана, а `Перейти к закрытию инцидентов` открывает существующую анкету `Закрытие инцидента`; при восстановлении уже активной сессии и в admin preview окно не показывается.
- [x] Анкета `Оборудование` получила дневной рабочий поток диспетчера: `Дата отчета` вынесена в отдельную глобальную настройку отчёта, UI показывает, какое оборудование внесено в дневной пакет за выбранную дату и какие позиции уже сохранены в БД именно за эту дату, переключение даты не переносит статус внесённой иконки на другой день, переключение оборудования не переносит значения между позициями, каждое оборудование добавляется отдельно кнопкой `Внести данные`, а ошибки заполнения после этой кнопки показываются красной подсказкой; кнопка `Отправить отчет начальству` активна только когда внесены все позиции оборудования и отправляет их одним отчётом; состояние `внесено в отчёт` выделяется отдельно от серверно сохранённого состояния, правки уже внесённой позиции помечаются `*` как несохранённые, кнопка становится `Обновить данные`, а при переключении оборудования предупреждение предлагает только `Сбросить` правки или `Остаться`, без автоматического применения; backend и локальный fallback отклоняют неполный дневной пакет, повторная отправка становится `Внести изменения в отчёт`, обновляет существующие записи за ту же дату отчёта и оборудование и пишет запись изменения в `dispatcher_equipment_report_revisions`, а напечатанные поля по каждому оборудованию сохраняются в браузере как UX-черновик выбранной даты даже без внесения в отчёт и без сохранения старой даты при повторном входе.
- [x] Сетка диспетчерских анкет выровнена: короткие поля `input/select` больше не растягиваются по высоте соседних `textarea`, сначала выводятся компактные поля, а большие текстовые поля располагаются внизу формы.
- [x] Поля `Место (цех/участок)` и `Ответственный за регистрацию` в анкете `Открытие инцидента` читаются backend-ом из Google Sheets через `GET /api/dispatcher/forms`; источник поддерживает `public_csv` и закрытую таблицу через service account JSON, URL таблицы, колонки и TTL кеша настраиваются через env, `GOOGLE_SHEETS_CACHE_TTL_MS=0` перечитывает справочник при каждом запросе форм, инструкция вынесена в `docs/google-sheets-source.md`. При обычном входе ответственным автоматически выбирается `displayName` текущего диспетчера и при необходимости временно добавляется в варианты формы, даже если его нет в справочнике; в admin preview выбирается первый вариант Google Sheets.
- [x] Email- и MAX-уведомления для открытия/закрытия инцидента, дневного отчёта оборудования и входа/выхода посетителя подключены к backend: email-адресаты инцидентов/оборудования читаются из Google Sheets колонки `Адресаты по инцидентам и оборуджованию (емейлы)`/`Адресаты по инцидентам и оборудованию (емейлы)`, MAX ID — из колонки `Чаты пользователей` или `ТОКЕН МАКС и Чаты пользователей`; строки `2–20` получают общие уведомления, `22–25` добавляются для механической причины простоя или инцидента, `27–30` для электрической. При закрытии инцидента backend переносит из сохранённого открытия дату/время, место, тип, критичность и описание в payload закрытия, а email/MAX-сообщение показывает после номера все эти данные, затем дату/время закрытия и утвердившего. Локальный test fallback повторяет перенос, а все уведомления backend-среды `SMB_APP_ENV=test` сохраняют основной формат и заканчиваются примечанием `Тестовое сообщение`; production-тексты остаются без этой пометки. Посетительские уведомления читаются отдельно из колонок `Адресаты по посетителям (емейлы)` и `Адресаты по посетителям (МАКС)` строк `2–20` и уходят только по формам `Вход посетителя`/`Выход посетителя`. В одной MAX-ячейке можно указать несколько ID через пробел, запятую, точку с запятой, новую строку или `|`; отчёт оборудования уходит одним сообщением на все заполненные позиции, повторная отправка помечается как изменение отчёта. SMTP и MAX bot token включаются через `server/.env`, а при недоверенной TLS-цепочке MAX на Jino используется точечный `MAX_CA_CERT_FILE`.
- [x] Доставка MAX-уведомлений сделана пофамильно проверяемой, потому что часть адресатов из конца списка не получала сообщения. Каждый ID получает отдельный последовательный запрос вместо одного параллельного пакета, ответы `429` и `5xx` повторяются до трёх попыток с нарастающей паузой, а `4xx` считается окончательным отказом. Отказ одного адресата больше не отменяет остальных: backend всё равно обходит весь список, пишет отдельную строку `*.max_recipient_failed` с ID и ответом MAX, затем `*.max_sent` с `recipientCount` и `deliveredCount`, и только после этого поднимает общий `AggregateError` с перечислением неудачных ID. Токен бота, лежащий в первой строке той же MAX-колонки таблицы, больше не считается адресатом: парсер отбрасывает значения длиннее 32 символов, а отправка дополнительно пропускает значение, равное `MAX_BOT_TOKEN`. ID, введённый в Google Sheets как число с разрядными пробелами (`334 864 352`), читается как один адресат, а не как три обрывка; несколько ID в одной ячейке по-прежнему разделяются пробелом, запятой, точкой с запятой, новой строкой или `|`.
- [x] Отправленный диспетчером отчёт `Выработка` уходит по email и MAX той же базовой группе адресатов строк `2–20`, что и отчёт по оборудованию; механические и электрические спецгруппы остаются привязаны только к соответствующим причинам оборудования и инцидентов.
- [x] Пользовательские уведомления и статусные подсказки для владельца, работника и диспетчера укорочены: тосты отправки показывают только результат действия, ошибки скрывают техническую диагностику, а email/MAX-сообщения по диспетчерским событиям оставляют только ключевые поля.
- [x] Владелец во вкладке `Обзор` видит сводку с начала текущего месяца: общее количество открытых инцидентов, сколько из них закрыто, сколько открыто сегодня и сколько остаётся незакрытыми по всей доступной истории; отдельный блок лаборатории показывает количество сохранённых результатов испытаний за месяц и за сегодня. Эти расчёты выполняет защищённый backend endpoint `GET /api/business/overview` по capability `business.view_all_statistics`; frontend только проверяет контракт и отображает готовые числа. В той же вкладке сохранены прежние компактные блоки оборудования, общей выработки текущего месяца и последних посетителей с количеством невышедших, которые строятся из общей сохранённой dispatcher-истории; удалены только подробные карточки последнего открытия и закрытия инцидента, поскольку их заменил новый блок `Инциденты`. Во вкладке `Диспетчерская` владелец выбирает один из 4 разделов: `Выработка`, `Оборудование`, `Инциденты`, `Посетители`, а единый переключатель периода предлагает `Сегодня`, текущие неполные месяц/год и `Своё`; пользовательский период по умолчанию не содержит дат и показывает всю историю, которую клиент автоматически дочитывает через backend-пагинацию, после чего обычный polling обновляет только последнюю страницу. `Выработка` содержит отдельные таблицы формовки, сортировки, неформованной продукции, шамота, замеров банок и грануляции с серверно рассчитанными месячными итогами; если открытый период был полностью пустым и polling впервые приносит строки, UI автоматически выбирает первую таблицу с данными, не оставляя активной пустую `Формовку`, а после появления данных сохраняет ручной выбор пользователя. Оборудование отображается агрегатами по названию, выработке, простою и причинам; клик по названию оборудования открывает расшифровку дневных строк выбранного периода с выработкой, простоем, причинами, примечаниями и временем обновления; инциденты объединяют открытие/закрытие и показывают открытые/закрытые с учётом незакрытых к началу диапазона; посетители показываются списком входов за выбранный период с временем выхода или пометкой, что выход не отмечен.
- [x] В руководительской `Диспетчерская → Выработка` отметка времени обновления остаётся сразу под общими фильтрами, а счётчик строк перенесён под кнопки производственных таблиц и показывает число строк текущей выбранной таблицы. Перед заголовками каждой таблицы добавлена выделенная строка итогов: backend по выбранному периоду суммирует суточные и остальные аддитивные показатели, для накопительных месячных показателей берёт значение строки самой поздней даты и возвращает типизированные итоги вместе с таблицами; frontend только отображает готовые числа, а текстовые колонки отмечает прочерком. При смене периода frontend отдельным безопасным запросом `productionDateFrom`/`productionDateTo` обновляет только server-owned итоги и не ограничивает общую историю, из которой строятся остальные разделы. Явно помеченный локальный test fallback повторяет расчёт без превращения браузера в production-источник аналитики.
- [x] Все пять разделов владельческого `Обзора` используют единый быстро считываемый карточный стиль. В `Оборудовании` дата последнего отчёта вынесена из карточек показателей в компактную подпись рядом с названием раздела, а отдельные карточки показывают числа работающих групп; `Выработка` показывает общий факт текущего месяца в тоннах. В `Посетителях` дата последнего посещения также находится рядом с названием раздела, а карточки показывают количество посетителей за этот день и число невышедших сейчас; ненулевое число невышедших получает тот же предупредительный акцент, что и незакрытые инциденты. Время обновления оборудования и список адресатов посещений остаются короткими вторичными подписями без длинных описательных предложений.
- [x] В разделе истории `Посетители` завершённые визиты сортируются по фактической дате и времени выхода по убыванию, поэтому последние выходы находятся сверху; визиты без отмеченного выхода показываются после завершённых.
- [x] Число незакрытых инцидентов в `Обзоре` и список кнопки `Все незакрытые` во вкладке `Диспетчерская` больше не могут расходиться: оба берут один server-owned список `openIncidents`. Backend считает его в `buildOpenIncidentSummaries` по всей истории открытий и закрытий, независимо от `limit`/`offset`/периода запрошенной страницы ленты, и возвращает вместе с `GET /api/dispatcher/submissions`; `openNow` в `GET /api/business/overview` — длина того же списка. Frontend только отображает пришедшие строки и строит из них кнопки выбора в анкете `Закрытие инцидента`, поэтому анкета тоже видит старые незакрытые инциденты и запрашивает ленту с минимальным `limit`. Клиентский расчёт незакрытых остался только в локальном тестовом `localStorage` fallback; в разделе `Инциденты` за выбранный период таблица по-прежнему строится из истории ленты вместе с закрытыми записями.
- [x] Админский аккаунт получил три вкладки: `Просмотр аккаунта` всегда показывает отдельный полный каталог поддерживаемых должностей и отдельно обновляемый список созданных учётных записей, включая добавленные в текущей сессии. Неадминистративные карточки открывают реальные рабочие кабинеты со всеми функциями выданного доступа, административные карточки видимы, но не запускают превью; после выбора основной shell полностью переключается на вид выбранного пользователя, помечается жёлтой рамкой, а надпись `АДМИН ПРЕВЬЮ МОД` показывается слева сверху рядом с иконкой сервиса. Если у выбранного пользователя нет доступных вкладок, превью показывает пустую белую рабочую область и не подставляет `Обзор`. В preview-режиме кнопка выхода подписана `Выйти из превью мода`, корневой body не скроллится и не оттягивается, прокрутка идёт внутри рабочей области. `БД` через backend API показывает только безопасные пользовательские разделы и поля; отдельное окно строится по server-owned типам/вариантам ввода и для диспетчерской записи показывает реальные поля её формы без ID, raw payload и технического `NULL`. Доступ к API проверяется на сервере по capability `platform.manage_analytics_database`. Исправление открытия инцидента через этот редактор переносит дату/время, место, тип, критичность и описание в сохранённое закрытие того же номера, поэтому снятый при закрытии снимок не остаётся с устаревшим текстом. Диспетчерские записи показывают номер инцидента отдельной колонкой, а над таблицей есть строка поиска: backend ищет совпадение по всем показанным колонкам раздела, сравнивая статус по русской подписи и дату по напечатанному дню, и возвращает количество строк и страницы уже по найденному набору.
- [x] Добавлен server-backed каталог должностей: должность хранит название, вкладки и capabilities, а аккаунт наследует их по `position_code`. Должности используют единый каталог рабочих и административных вкладок; системная административная должность защищена от создания и редактирования через UI. Миграция `010_dynamic_account_positions` переносит семь базовых должностей в каталог.
- [x] В разделе `Должности и доступы` порядок сохраняется автоматически полным server-owned списком через 5 секунд после последнего перемещения. Каждое новое нажатие `Выше`/`Ниже` сбрасывает задержку, поэтому серия действий объединяется в один запрос; ручная кнопка сохранения удалена, а `Отменить` очищает ещё не отправленный порядок. Ожидающий порядок сохраняется при фоновом обновлении аккаунтов, изменение состава должностей до отправки блокируется, а после сетевой или серверной ошибки `5xx` попытка автоматически повторяется через 5 секунд.
- [x] Настройка вкладок перенесена из строк аккаунтов в компактную таблицу должностей; изменение должности отзывает сессии всех связанных аккаунтов. У аккаунта вкладки показываются только для чтения, а при создании выбирается одна из актуальных должностей.
- [x] Поле `Базовый кабинет` удалено из формы, клиентского контракта и API сохранения должности. Форма всегда показывает все шесть рабочих вкладок и требует выбрать хотя бы одну.
- [x] Backend разрешает любой набор рабочих вкладок любой неадминистративной должности и выводит capabilities только из выбранных пунктов. Канонический аккаунт `admin` дополнительно может включить в такой набор `admin.*`; сервер запрещает остальным пользователям менять этот поднабор и назначать уже привилегированную должность аккаунту. В форме должности переключатель `Админ` расположен рядом с заголовком окна; обычные пункты собраны в секции `Рабочие вкладки`, а включаемые переключателем административные пункты — в отдельной секции `Административные вкладки`. Единый frontend workspace открывает и переключает выбранные рабочие и административные вкладки независимо от исторического технического `accountType`.
- [x] Форма создания учётной записи не содержит полей организации или scope: все неадминистративные аккаунты автоматически принадлежат единой организации. Таблица аккаунтов сортируется по названию должности и затем по имени, поэтому одинаковые должности отображаются подряд.
- [x] При изменении названия или вкладок должности repository сохраняет её внутренний технический тип, оставляет связанные неадминистративные аккаунты в organization scope и отзывает их активные сессии. Системные неадминистративные должности можно настраивать, системного администратора — нельзя.
- [x] Любую неиспользуемую неадминистративную должность, включая созданную программно системную должность, можно удалить из таблицы после подтверждения. Кнопка недоступна для администратора и при `usageCount > 0`; API и repository повторяют проверки независимо от `isProtected`, а миграция `013_protect_used_account_positions` добавляет внешний ключ `account_accesses.position_code → account_positions.id` с `ON DELETE RESTRICT` для защиты от гонок.
- [x] Учётную запись можно удалить из admin-таблицы после подтверждения. Удаление является безопасным архивированием: backend переводит пользователя в `archived`, деактивирует его account access, отзывает сессии и сохраняет исторические отчёты; текущую учётную запись удалить нельзя.
- [x] В каталоге `Типы аккаунтов` карточки показывают только название должности без повторения его как имени пользователя и без служебной подписи `Типовой кабинет`; у администратора остаётся только пометка `Без превью`. Карточки реальных созданных аккаунтов по-прежнему показывают должность, имя сотрудника и логин.
- [x] Тестовый экран `Выбор доступа` больше не ограничен четырьмя техническими кабинетами: он получает актуальные должности из server-backed каталога через dev-only `GET /api/dev/access-session`, а выбранная должность открывается с её текущими вкладками и capabilities. Локальный Vite/client fallback показывает актуальные базовые должности и корректно входит даже когда remote `access/profile` отвечает пустым профилем; production-auth и отключение dev endpoint не изменены.
- [x] Test/production разделение реализовано как две среды одного кода: frontend читает `VITE_SMB_APP_ENV`, backend читает `SMB_APP_ENV`, test сохраняет role-picker/dev-session/local fallback, production показывает форму логина/пароля, отключает `/api/dev/access-session`, запрещает клиентский auth/data fallback и использует отдельные cookie/env/БД. Добавлены auth-таблицы, `POST /api/auth/login`, `POST /api/auth/logout`, server-side session cookie, `scrypt` password hashing без новой зависимости, CLI `npm --workspace server run auth:create-user` для первого пользователя и server-side capability checks для dispatcher/admin endpoints. В production диспетчерский payload не содержит tenant/business ID, а `submittedByAccountId` определяется только из authenticated server session.
- [x] В test-админке добавлена полная синхронизация БД из production: отдельный server-gated endpoint и опасная UI-карточка доступны только при `SMB_APP_ENV=test`, capability `platform.manage_analytics_database` и явной server env-настройке. Backend подключает production только как read-only source, читает его в consistent snapshot, требует одинаковые таблицы/DDL/`schema_migrations`, отсутствие триггеров и InnoDB для всех таблиц, затем в одной test-транзакции заменяет строки всех таблиц данными production и оставляет `auth_sessions` пустой. Общий межпроцессный advisory lock дожидается уже начатых runtime-записей, migration/auth CLI и не допускает их пересечения с заменой; при любой ошибке MariaDB/MySQL откатывает всю транзакцию без частично заменённой БД. Production UI/endpoint отключены, источник и точное имя test-БД защищены отдельными env-guard, а test deploy preflight требует их настройки.
- [x] Диспетчерские сессии ежедневно завершаются в `07:45 МСК`: backend ограничивает срок production-сессии ближайшей московской границей, dev/test профили получают такой же `expiresAt`, просроченные dev-сессии отклоняются, а открытый frontend запускает штатный logout по таймеру. Каждое MariaDB/MySQL-соединение backend-а явно переводится в UTC, чтобы Jino `SYSTEM = UTC+3` не сдвигал абсолютный `expires_at` на три часа; regression-тест проверяет инициализацию нового соединения. Остальные типы аккаунтов сохраняют обычный настроенный TTL.
- [x] Админская вкладка `Учётные записи` показывает таблицу аккаунтов на всю ширину, а форму создания открывает отдельной кнопкой в модальном окне. Backend генерирует `userId`/`accessId`, автоматически назначает неадминистративным аккаунтам organization scope и не принимает business/tenant override. Администратор может менять должность отдельной кнопкой; backend берёт кабинет, вкладки и capabilities из server-owned должности, преобразует platform/organization scope, отзывает сессии и пишет audit-событие; собственную должность менять запрещено. Повторный логин отклоняется без изменений; сброс пароля и временное отключение входа остаются отдельными server-gated действиями. Нативные `select` имеют стабильное CSS-оформление.
- [x] Jino deploy подготовлен для варианта "одна ветка, две frontend-сборки": добавлены `npm run build:web:test`, `npm run build:web:production`, `npm run deploy:jino:dual`, env-шаблоны `.env.test.example`, `.env.production.example`, `server/.env.test.example`, `server/.env.production.example` и скрипт `scripts/deploy-jino-dual-env.sh`. Скрипт деплоит `test.smb.aonmou.ru` и `smb.aonmou.ru` из одной ветки, но сохраняет разные runtime-каталоги backend, env, cookie, CORS и БД; backend-команды могут читать явный env-файл через `SMB_SERVER_ENV_FILE`.
- [x] Для Jino deploy добавлен локальный VS Code extension `SMB Jino Deploy`: status bar показывает выбранную ветку (`Dev` по умолчанию) и отдельную актуальность test/production, меню запускает deploy одной или обеих сред и позволяет сменить локальную ветку. Незакоммиченные и untracked локальные файлы не блокируют deploy и не копируются на сервер: публикуется только синхронизированный commit выбранной ветки из `origin`. Во время deploy остаётся одно стандартное уведомление VS Code с progress bar по фактически достигнутым этапам SSH, preflight, тестов, миграций, сборки, публикации и smoke-проверок; Output-журнал заполняется без автоматического открытия и остаётся доступен из меню. Обычные статусы и ошибки не окрашивают нижнюю панель; отклонённый Jino публичный ключ показывается как понятное состояние `Нужен SSH-ключ`, а отдельное действие копирует идемпотентную команду для встроенной консоли Jino, которая добавляет ключ в `~/.ssh/authorized_keys` с правами `0700/0600` без удаления существующих ключей. Если verbose SSH-проверка подтверждает, что сервер принимает ключ, но сразу закрывает соединение, extension показывает отдельное состояние `Настройте SSH`, не предлагает повторно устанавливать ключ и направляет пользователя проверить услугу `Поддержка SSH`, SSH-пароль и разрешённый IP в `Управление → SSH-подключение`. Кнопка открывает отдельную панель хостинга `https://cp-hosting.jino.ru/`, а не общий корень `https://cp.jino.ru/`; для первичной установки ключа внутри выбранного контейнера пользователь переходит в `Управление → Консоль`. Нестабильный `ssh-copy-id` не считается основным setup-путём. Источником статуса служит `.smb-deploy-state`, который каждая среда получает только после успешной публикации frontend и restart backend; marker сравнивается с последним commit выбранной ветки в `origin`, поэтому обновлённый checkout после неудачной сборки не считается опубликованной версией.
- [x] В frontend deploy-артефакт добавлен tracked `public/.htaccess`: он принудительно перенаправляет HTTP на HTTPS с учётом Jino `X-Forwarded-Protocol`, добавляет HSTS и базовые security headers, а deploy проверяет и публикует dotfiles. Live-проверка 11 июля 2026 года подтвердила действующие Let's Encrypt сертификаты, но до следующего deploy оба HTTP-домена всё ещё отвечают `200` вместо `301`; это также блокирует `access/profile` из HTTP-origin по CORS.
- [x] Шаг 1: базовая серверная модель аккаунтов и данных подготовлена на уровне frontend-контрактов и boundary.
- [x] Шаг 2: визуальный shell реально запрашивает `access/profile` через клиентскую boundary-функцию и локальный dev server.
- [x] Шаг 3: матрица capabilities используется и на клиенте для UI-gating, и на backend для dispatcher/admin действий.
- [x] Добавлена назначаемая должности вкладка `Огнеупорный цех` (`business.refractory_shop`) с единым меню трёх независимых сменных таблиц: `ЦОШ`, `Сводка по работе оборудования`, `Печное отделение`. Структура и видимые формулировки всех трёх экранов повторяют бумажные бланки: ЦОШ идёт в исходной последовательности операций, в сводке оборудования `Простой всего` стоит перед причинами, выпуск выделен светло-зелёным, простои — светло-красным, а первые две колонки сужены; в печном отделении `Брак всего` расположен перед видами брака, используется термин `Выплавка`, а сменные поля называются `Время прогонки` и `Присутствуют на смене`. В таблицах формованных, неформованных и обожжённых огнеупоров есть итоговые строки, которые пересчитываются при вводе; backend остаётся источником итогов сохранённой ревизии. Таблица выпуска обожжённых огнеупоров раскрывается на полную высоту без внутренней вертикальной прокрутки и сохраняет отдельную горизонтальную полосу для широких колонок. Каждая таблица хранится по дате начала смены и номеру смены, получает server-owned имя мастера из активной сессии, проходит строгую серверную валидацию и получает read-only итоги, рассчитанные backend-ом. Большая таблица оборудования использует отдельную компактную сетку колонок: числовые поля уже текстовых, длинные заголовки переносятся, а первый столбец `Оборудование` закреплён слева при горизонтальной прокрутке. Во всех числовых полях посторонние символы отбрасываются при вводе, запятая нормализуется в точку, целые значения, server-owned диапазоны и точность до трёх знаков после запятой проверяются до отправки; неверные поля получают красную подсветку и `aria-invalid`, а ошибка различает формат, диапазон и точность, использует русские названия колонок без технических ключей payload и показывает общий заголовок с каждой отдельной ошибкой на новой строке. Backend повторяет те же проверки, возвращает понятные русские сообщения вместе со стабильным `fieldPath` в безопасном типизированном массиве и позволяет точно подсветить поля после отклонённого POST, включая затарку и динамические строки с пустыми промежутками. Отправка создаёт отдельную pending-ревизию; диспетчер во вкладке `Форма` видит отдельную кнопку `Таблицы огнеупорного цеха` со счётчиком ожидающих решений, а полная очередь открывается только после нажатия этой кнопки и не смешивается с обычными диспетчерскими формами. Badge левого rail и фоновое toast-уведомление сохраняются. Диспетчер отдельно подтверждает или возвращает каждую таблицу с обязательным комментарием; после перехода в режим возврата кнопка `Подтвердить` скрывается. Аккаунт ОЦ раз в 5 секунд проверяет через отдельный server-gated endpoint свои актуальные pending/rejected без временного ограничения и недавно подтверждённые ревизии: принятие и возврат показываются общими toast-уведомлениями во всём рабочем пространстве, при возврате выводится причина диспетчера, а открытая таблица сразу обновляет статус. В левом rail вкладка ОЦ показывает красный badge с количеством актуальных возвратов, требующих доработки, а внутри ОЦ этот итог разбивается по трём кнопкам: красная цифра появляется только у тех видов таблиц, где последняя ревизия действительно возвращена. Ожидающие, подтверждённые и неотправленные таблицы не создают такой badge; возврат перестаёт учитываться после повторной отправки более новой ревизии и не исчезает только из-за возраста. Pending и подтверждённая ревизии заблокированы от обычного редактирования, после подтверждения доступно явное создание исправления, а возврат открывает сохранённые значения для доработки. Все ревизии и решения сохраняются в MariaDB/MySQL миграцией `021_refractory_report_revisions` и пишутся вместе с обязательным audit-событием; собственную таблицу подтвердить нельзя. После commit новой pending-ревизии backend отправляет краткое уведомление адресатам строк `2–20` Google Sheets колонок `Адресаты Диспетчеры (емейлы)` и `Адресаты Диспетчеры (МАКС)`. После commit подтверждения отправляет полное содержимое таблицы и рассчитанные итоги адресатам колонок `Адресаты ОЦ (емейлы)` и `Адресаты ОЦ (МАКС)` через существующие SMTP/MAX адаптеры; возврат наружу не рассылается, а ошибка канала не откатывает сохранение или решение. Аналитика подтверждённых таблиц пока не строится.
- [x] В режиме явного исправления подтверждённой таблицы ОЦ доступна кнопка `Отменить`: она без API-запроса отбрасывает несохранённый черновик, возвращает последний подтверждённый снимок и снова блокирует поля.
- [x] В существующий лабораторный workspace добавлена внутренняя вкладка `Банки`. Лаборант выбирает банку I–III и материал; насыпной вес рассчитывает backend, а append-only назначение с автором и временем хранит миграция `024_laboratory_bank_assignments`. Источником веса сначала был результат `Контроля готовой продукции`, теперь — среднее последних записей `Журнала печи 2`. Текущие назначения и полная история видны лаборатории; диспетчер получает через отдельный защищённый read-only endpoint только номер банки и текущее наименование материала, а `Выработка → Замеры банок` информационно показывает его рядом с номером без добавления в payload дневного отчёта. В прежнем разделе `Замеры банок` таблицы `Сводка по ЦОШ (ежесменная)` одновременно отображаются все три банки: по умолчанию открыто четыре поля, но для каждой банки можно независимо добавлять и удалять замеры, сохраняя минимум один. Backend блокирует отправку без назначения или замера любой из трёх банок, читает живую таблицу `H·m → M3` из Google Sheets `Банки`, линейно интерполирует объём по среднему замеру и умножает его на насыпной вес назначения. Марка, источник насыпного веса, назначение, среднее, объём, коэффициент и масса сохраняются снимком в append-only ревизии ЦОШ и попадают в уведомление; нулевой результат разрешён, отдельное состояние полного опустошения пока не моделируется.
- [x] В лабораторный workspace добавлен `Журнал контроля параметров обжига вращающейся печи 2`: форма сохраняет дату, время, производимый материал, одиннадцать числовых параметров, мастера смены, обжигальщика, лаборанта и необязательное примечание в отдельной append-only таблице MariaDB/MySQL миграции `030_rotary_kiln_2_firing_journal`. Запись и обязательное audit-событие проходят одной транзакцией под capability `business.manage_laboratory_results`. История фильтруется на backend по периоду и поиску по сотрудникам/примечанию; endpoint возвращает не более 200 последних подходящих строк и средний насыпной вес ровно по этой отображаемой выборке. Frontend показывает расчёт `Средний насыпной вес` над фильтрами и широкую накопительную таблицу всех полей.
- [x] В лабораторный workspace добавлены связанные `Журнал регистрации отбора проб` и `Журнал химических анализов`. Регистрация сохраняет только реквизиты отбора в append-only таблице миграции `031_laboratory_sample_registration_journal`. Для химического анализа лаборант выбирает зарегистрированную пробу из server-owned списка по коду и передаёт её внутренний `id`; backend повторно проверяет связь. Обязательными остаются только зарегистрированная проба и номер партии; дата анализа, лаборант, семь текстовых показателей и примечания необязательны. Анализы хранит отдельная append-only таблица миграции `032_laboratory_chemical_analysis_journal`, а миграция `033_optional_laboratory_chemical_analysis_values` делает необязательные колонки nullable. Список проб имеет отдельный server-side поиск, поэтому доступны старые записи за пределами стартового окна, а совпадающие коды различаются по датам отбора и регистрации. Текстовый формат показателей поддерживает запятые и квалификаторы вроде `< 0,1`. Обе записи проходят с обязательным audit-событием одной транзакцией под capability `business.manage_laboratory_results`; оба журнала имеют backend-фильтры по профильной дате и поиску. История регистрации подтягивает последний связанный химический анализ, сохраняя прежние вручную введённые значения только как legacy fallback.
- [x] В `Регистрации проб` поле `Место отбора пробы` использует редактируемый раскрывающийся список из десяти согласованных начальных мест. Произвольное новое место разрешено существующей серверной валидацией и после сохранения остаётся в server-owned записи. Отдельный manage-only endpoint один раз при открытии формы возвращает все уникальные места из полной истории независимо от 200 отображаемых строк; миграция `036_sample_registration_sampling_location_index` добавляет составной индекс места и времени создания, а frontend объединяет серверные значения с начальными вариантами без дубликатов по регистру и лишним пробелам.
- [x] В `Регистрации проб` добавлено обязательное для новых записей поле `Водопоглощение`. Значение сохраняется текстом с поддержкой десятичной запятой, включается в обязательный audit-снимок и отображается в общей таблице журнала для лаборанта и руководства. Миграция `037_sample_registration_water_absorption` добавляет nullable-колонку к append-only реестру, поэтому исторические записи без показателя остаются доступными и показывают `—`.
- [x] Все поля `Марка изделия` в таблицах ОЦ, диспетчерской выработке и админском редакторе диспетчерской записи используют общий защищённый endpoint `/api/production-brands` и единую вкладку Google Sheets `Номенклатура`: backend читает только колонку `Наименование`, игнорирует `Вид изделия`, проверяет и канонизирует марки перед сохранением обоих видов отчётов и административного изменения. Frontend использует общий полноширинный поисковый компонент выбора; ручное добавление вынесено из повторяющихся ячеек в один общий контрол перед таблицами каждой рабочей формы и разрешено только в production. На тестовом сайте контрол создания скрыт, а backend при `SMB_APP_ENV=test` отклоняет `POST` до обращения к Google Sheets; чтение и выбор марок остаются доступными. В production backend записывает новую уникальную марку в первую свободную ячейку `Номенклатура!A:A` сверху, фиксирует обязательный audit и компенсирующе очищает эту ячейку при сбое audit-транзакции. Сохранённая ранее марка остаётся в ревизии как историческое значение; недоступность Google Sheets показывается отдельной ошибкой без подмены данных клиентским fallback.
- [x] В `Сводке по ЦОШ (ежесменная)` фиксированные поля выпуска `ШБО`, `ШГР-1`, `ШГР-2`, `ШКИ` заменены динамической таблицей до 50 строк. В каждой строке мастер выбирает уникальную марку через общий поиск `Номенклатура!A:A` и указывает выпуск в тоннах; неполные пары и повтор марки отклоняются frontend-ом и backend-ом, а backend канонизирует марку до сохранения и считает общий итог по строкам. Если марка строки отсутствует в номенклатуре, backend возвращает раздел `Выпуск шамота`, номер строки, название поля и ошибочное значение; frontend выделяет и фокусирует соответствующее поле. Подтверждённое Email/MAX-уведомление перечисляет введённые марки и тоннаж. Старые ревизии с четырьмя прежними полями продолжают отображаться и форматироваться как исторические данные.
- [x] Добавлен первый server-backed контур `Лаборатория`: системная должность `Лаборант` использует существующий неадминистративный `accountType`, получает одну вкладку `Результаты испытаний` (`business.laboratory_results`) и capability `business.manage_laboratory_results`. Внутри вкладки переключаются `Входящий контроль` и `Контроль готовой продукции`; виды готовой продукции, полный каталог показателей и ГОСТ backend читает из живой вкладки Google Sheets `Лаборатория`, а frontend строит все поля динамически без fallback-списка и всегда держит их открытыми. Во входящем контроле `Объект испытаний` вводится свободным текстом и не сверяется со справочником; один отчёт хранит общие дату и контекст документа/транспорта, а также одну или несколько проб с собственными идентификаторами и хотя бы одним показателем у каждой. Цель испытаний и примечание к протоколу обязательны для обоих разделов, при этом цель получает редактируемое начальное значение. Готовая продукция дополнительно выбирает каноническую марку из общей `Номенклатура!A:A` и требует хотя бы один показатель. Лаборант всегда определяется серверной сессией, внешние справочники проверяются до транзакции, а результат и обязательное audit-событие сохраняваются вместе в append-only таблицу `laboratory_results` миграции `023_laboratory_results`. История доступна в той же вкладке с фильтрами по объекту испытаний либо марке; старые входящие записи с одной пробой читаются как отчёты из одной пробы. Все таблицы истории используют видимую сетку ячеек и горизонтальную прокрутку для полного набора показателей. Для каждого результата история предлагает открыть защищённый PDF-протокол в просмотрщике, откуда его можно распечатать или сохранить: server-owned шаблон повторяет постоянную шапку предоставленного бланка, условные строки и ГОСТы следуют профилю из матрицы, а пробы, значения и лаборант берутся из сохранённого результата. Снимок матрицы сохраняется вместе с новым результатом, поэтому последующее изменение Google Sheets не меняет исторический протокол; legacy-записи используют текущую матрицу.
- [x] Добавлен раздел просмотра `Лаборатория` для руководства: отдельная назначаемая вкладка `business.laboratory_review` с capability `business.view_laboratory_results`, которую администратор выдаёт нужным должностям наравне с остальными вкладками. Вкладка только читает данные: backend по этой capability пропускает лишь `GET /api/laboratory/reference`, `GET /api/laboratory/results` и защищённый PDF-протокол, а сохранение результатов, банки и лабораторные журналы остаются за `business.manage_laboratory_results`. Фильтрация состоит из одного списка кнопок области поиска, кнопки `По дате испытаний` с периодом `С даты`/`По дату` и кнопки `По наименованию (номенклатуре)` с поиском; выключение кнопки очищает её поля. Поиск по наименованию выполняет backend новым параметром `name` подстрокой сразу по объекту испытаний и марке с экранированием шаблонов LIKE. Таблица результатов вынесена в общий `src/LaboratoryResultsTable.tsx` и используется обеими вкладками. Так как лабораторные таблицы разноформатные, вкладка ищет сразу по всем журналам: область поиска выбирает один список кнопок `Все испытания`, `Регистрация проб`, `Химические анализы` и `Журнал печи 2`, причём последние три открываются вложенным рядом из кнопки `ЦЗЛ` — так же, как на вкладке лаборанта. В режиме `Все испытания` подходящие журналы выводятся отдельными таблицами одна за другой со своим заголовком в порядке кнопок. Журнал показывается, только если поддерживает все включённые фильтры: наименование есть у регистрации проб и химических анализов, период применяется ко всем, но каждый журнал фильтрует свою дату (регистрации, химического анализа или записи печи). Остальные журналы не скрываются молча — они перечислены с причиной в примечании под фильтрами, а сам отбор считает чистый `src/services/laboratoryReviewJournals.ts`. Backend по capability просмотра дополнительно пропускает `GET` трёх журналов, оставляя их `POST`, банки и остальные endpoint за `business.manage_laboratory_results`, и принимает у журналов проб параметр `name` подстрокой только по `sample_name` с экранированием LIKE. Таблицы трёх журналов вынесены в общий `src/LaboratoryJournalTables.tsx`, поэтому у журнала один формат колонок и для лаборанта, и для руководителя.
- [x] Журналы ЦЗЛ отделены от разделов контроля в лабораторном workspace: корневой ряд вкладок содержит `Банки` и `ЦЗЛ (Центральная заводская лаборатория)` — разделы `Входящий контроль` и `Контроль готовой продукции` из него скрыты после перехода на журнальный расчёт насыпного веса, — а `Журнал печи 2`, `Регистрация проб` и `Химические анализы` открываются только вложенным рядом внутри `ЦЗЛ`. Порядок кнопок группы задаётся одним типизированным списком в `src/LaboratoryResults.tsx`, при открытии `ЦЗЛ` показывается первый журнал группы, а выбранный журнал сохраняется при возврате в группу. Руководительская вкладка `Лаборатория` разделена так же: корневой ряд оставляет `Все испытания` и кнопку `ЦЗЛ`, а области поиска `Регистрация проб`, `Химические анализы` и `Журнал печи 2` открываются вложенным рядом. Принадлежность области к ряду задаёт поле `group` в `src/services/laboratoryReviewJournals.ts`, откуда выводятся оба списка кнопок; порядок журналов внутри группы совпадает с порядком таблиц в режиме `Все испытания`. Общая подпись кнопки хранится один раз в `src/LaboratoryJournalTables.tsx` рядом с таблицами тех же трёх журналов.
- [x] Насыпной вес банок перестроен на `Журнал печи 2`. В журнал добавлено обязательное поле `Производимый материал` сразу после `Времени`: марка выбирается общим поисковым списком из вкладки Google Sheets `Номенклатура`, backend канонизирует её до DB-транзакции и фиксирует в audit-событии, а миграция `034_rotary_kiln_2_produced_material_bank_density` добавляет nullable колонку `produced_material` с индексом по материалу и дате. Назначение банки больше не выбирает результат испытаний: лаборант выбирает материал, а backend считает насыпной вес средним по последним десяти записям этого материала (пока записей меньше — по всем накопленным) и сохраняет вместе с назначением источник `rotary_kiln_2_journal` и количество усреднённых записей. Та же миграция снимает внешний ключ на результат испытаний, делает `laboratory_result_id`, `sample_index` и `sample_identifier` nullable и добавляет `bulk_density_source`/`bulk_density_sample_count`, поэтому прежние назначения остаются читаемыми как `laboratory_result` и продолжают учитываться в расчёте ЦОШ и в уведомлениях. Разделы `Входящий контроль` и `Контроль готовой продукции` скрыты из корневого ряда вкладки лаборанта — там остаются только `Банки` и группа `ЦЗЛ`; их код сохранён и возвращается списком `visibleControlSections` в `src/LaboratoryResults.tsx`.
- [x] Разделы `Входящий контроль` и `Выходящий контроль` убраны и у всех, кто просматривает лабораторные анализы: логика этих разделов признана некорректной и будет заменена другой логикой в других местах. В руководительской вкладке `Лаборатория` корневой ряд оставляет `Все испытания` и кнопку `ЦЗЛ`, а журнал `Результаты испытаний` уходит и из режима `Все испытания`, потому что состоит только из этих двух разделов; просмотр запрашивает лишь журналы регистрации проб, химических анализов и печи 2, поэтому вкладка больше не вызывает `GET /api/laboratory/results` и `GET /api/laboratory/reference`. Оба списка кнопок и список журналов выводятся из одного типизированного `visibleLaboratoryReviewSections` в `src/services/laboratoryReviewJournals.ts` — так же, как `visibleControlSections` на вкладке лаборанта, — поэтому код журнала, сохранённые результаты `laboratory_results` и их PDF-протоколы остаются на месте, а вернуть их в просмотр можно, снова перечислив разделы в этом списке. Серверные права не менялись: capability `business.view_laboratory_results` по-прежнему допускает `GET` результатов и защищённого протокола, чтобы будущая логика могла их переиспользовать.
- [x] Красные badge трёх внутренних кнопок ОЦ ограничены выбранными датой и сменой: возврат показывается только на соответствующем `reportDate + shiftNumber`, а при переходе на другую дату или смену исчезает. Общий badge вкладки ОЦ в левом rail остаётся глобальным, чтобы возврат не потерялся.
- [x] Под общим badge ОЦ в левом rail перечисляются уникальные даты и смены актуальных возвратов в формате `Исправить за ДД.ММ.ГГГГ · смена N`, чтобы пользователь сразу понимал, какой контекст выбрать; одинаковая дата и смена нескольких возвращённых таблиц показывается одной строкой.
- [x] Поле `Производимый материал` в форме `Журнала печи 2` автозаполняется предыдущим значением: как только полная выборка журнала возвращает хотя бы одну запись с материалом, форма подставляет материал самой свежей такой записи, а после успешного сохранения оставляет материал сохранённой записи и очищает остальные поля. Значение берётся только из невыфильтрованной выборки, чтобы фильтры истории по датам и поиску не подменяли подстановку срезом журнала; поле остаётся обычным поисковым списком номенклатуры, и первая же ручная правка отключает дальнейшую автоподстановку до следующего сохранения. Изменение полностью frontend-овое: контракт, серверная валидация и расчёт насыпного веса не менялись.
- [x] Широкие таблицы ОЦ получили ограниченные по высоте внутренние scroll-контейнеры: горизонтальная полоса постоянно оформлена, остаётся у нижней границы видимой таблицы и располагается в собственном нижнем пространстве, не перекрывая последнюю строку; закреплённые заголовки и первый столбец оборудования сохраняются.
- [ ] Ближайший следующий шаг: получить текст Положения и буквально сверить с ним поля формы поручения; выдать service account право `Editor` на основную Google Sheets книгу, заполнить строки `2–20` четырёх колонок адресатов ОЦ, задеплоить одинаковый код и миграции `021_refractory_report_revisions`–`034_rotary_kiln_2_produced_material_bank_density` в test/production. После deploy проверить на test новый путь насыпного веса: запись журнала печи 2 с производимым материалом → появление материала в выборе банки → назначение банки со средним по последним записям → расчёт массы в `Сводке по ЦОШ` и `Выработке` → колонка `Содержимое` в руководительской `Диспетчерская → Выработка → Замеры банок`. Там же проверить автоподстановку материала: при повторном открытии формы журнала поле заполнено материалом последней записи, ручная замена из списка работает и сохраняется именно выбранное значение. После deploy через штатную админскую настройку должностей выдать Ларикову А.Т., Самсонову Г.Н. и Глушкову Ю.В. вариант `Поручения Совета директоров (создание, приёмка и возврат на доработку)`, затем проверить на test живые вкладки `Лаборатория`, `Банки`, группу `ЦЗЛ` с журналами `Журнал печи 2`, `Регистрация проб`, `Химические анализы` и `Поручения Совета директоров`, ввод/фильтрацию трёх лабораторных журналов, просмотр руководителем нескольких журналов подряд в разделе `Лаборатория` с фильтрами периода и наименования и без разделов входящего/выходящего контроля, средний насыпной вес, выбор зарегистрированной пробы и подтягивание последнего химического анализа в историю регистрации, защищённые PDF-протоколы, четыре варианта доступа и переход повторяющегося поручения к следующей активной дате; параллельно проверить чтение марок, ручное добавление только на production и уведомления ОЦ. Отдельно после deploy закрыть жалобу на недоставку MAX: отправить тестовый вход посетителя и инцидент, затем в логе backend сверить `*.max_sent` (`recipientCount` должен совпадать с числом заполненных строк `2–20` соответствующей MAX-колонки) и `*.max_recipient_failed`; если ID Самсонова из строки 9 колонки `ТОКЕН МАКС и Чаты пользователей` доставлен без ошибки, а сообщение не пришло, причина вне backend — нужен начатый диалог этого пользователя с ботом и ID того типа, который задан в `MAX_RECIPIENT_ID_TYPE`. После deploy отдельно сверить, что показатель `Не закрыто сейчас` в `Обзоре` и число строк кнопки `Все незакрытые` во вкладке `Диспетчерская` совпадают, а анкета `Закрытие инцидента` предлагает те же инциденты. После этого продолжить настройку read-only production snapshot и остальные live-проверки планов, редактора БД и исторического импорта.

- [x] Диспетчерская форма `Выработка` при выборе даты загружает последнюю сохранённую за этот день сводку, восстанавливает факты, марки, динамические столбцы, банки и грануляцию и позволяет отправить изменения новой append-only версией. Одновременно она получает server-calculated накопительный план до выбранной даты включительно и месячный факт предыдущих дней по всем четырём категориям. Read-only `Факт за месяц` и `Отклонение` мгновенно пересчитываются во время ввода с учётом суммы черновых фактов выбранных суток, даже если отчёт за этот день ещё не сохранён. В admin preview форма также загружает сохранённую сводку, дневные и накопительные планы, месячные факты, отклонения и справочник марок вместо пустого демонстрационного состояния; руководительские таблицы продолжают использовать последнюю версию дня.
- [x] В `Выработка → Замеры банок` начало и конец дня разделены на `По замерам` и `По отгрузкам`. Защищённый dispatcher endpoint по выбранной дате возвращает текущее назначенное Лабораторией содержимое и средние замеры из подтверждённых ЦОШ ОЦ: начало `По замерам` берётся из последней подтверждённой ЦОШ предыдущей календарной даты, конец — выбранной даты; при нескольких сменах используется старшая смена и её последняя подтверждённая ревизия. Эти поля показываются read-only, pending/rejected таблицы не используются, а POST выработки повторно получает server-owned значения и заменяет ими любые клиентские `jarStart*`/`jarEnd*`. Значения `По отгрузкам` диспетчер вводит вручную в отдельных `jarShipmentStart*`/`jarShipmentEnd*`; они сохраняются и восстанавливаются вместе с отчётом.
- [x] В руководительской `Диспетчерская → Выработка → Замеры банок` рядом с номером банки показывается её текущее содержимое: отдельная колонка `Содержимое` берёт назначенный Лабораторией материал, а банка без назначения показывается как `Не назначено`. Список приходит с backend-а тем же способом, что и `openIncidents`: `GET /api/dispatcher/submissions` дополнительно возвращает server-owned `bankContents` с номером банки и наименованием материала, без насыпного веса и автора назначения. Отдельный dispatcher endpoint содержимого банок и capability не меняются, а локальный тестовый `localStorage` fallback отдаёт пустой список, поэтому в нём все банки показываются как `Не назначено`. Значение всегда текущее, а не историческое: назначения меняются редко, поэтому строки прошлых дат показывают материал, назначенный банке сейчас.

## Технические правила

- Не добавлять внешний auth provider, state manager, chart library, UI framework, SDK интеграций и новые зависимости без отдельной задачи и проверки текущих локальных возможностей.
- Держать типы аккаунтов, доступы, статусы, периоды и показатели типизированными и централизованными.
- UI должен быть тонким: отображение, ввод и вызов прикладной логики.
- Бизнес-правила, проверки, переходы статусов, права доступа и подготовку данных держать отдельно от JSX и CSS.
- Любые формы, импорты, подтверждения, исправления, доступы, аудит и интеграции считать security-sensitive и покрывать проверками.
- Не использовать реальные приватные данные в тестовом контенте интерфейса.
- Все реальные бизнес-данные, пользовательские данные, права, аналитика, подтверждения и история должны храниться и проверяться на сервере.
- Клиент хранит только типы контрактов, UI-состояние и статический текст интерфейса; итоговые данные и разрешения получает по запросу к серверу.
- В remote/server-PC/VPS режиме открывать в сеть только backend API; MariaDB/MySQL оставлять локальной для backend или доступной только по разрешённому IP и не публиковать порт `3306` наружу без отдельного решения.
- `docs/local-server-run.md` держать коротким runbook локального запуска: отдельно локальный Docker для MariaDB и отдельно локальный запуск backend/frontend.
- Внешние справочники из Google Sheets подключать через backend, а не через frontend; текущий Google Sheets источник для полей `Место (цех/участок)` и `Ответственный за регистрацию` должен работать либо через публичный CSV, либо через service account JSON для закрытой таблицы и документироваться в `docs/google-sheets-source.md`.
- Email- и MAX-рассылку диспетчерских событий выполнять на backend после успешного сохранения формы; адресаты должны читаться из Google Sheets фиксированными строками, SMTP-пароли и MAX bot token хранить только в `server/.env`, а ошибка отправки уведомления не должна отменять уже сохранённую dispatcher submission. Отчёт оборудования рассылать одним кратким дневным batch-сообщением после полного `/api/dispatcher/equipment-report`, а не отдельным уведомлением на каждую позицию.
- Справочники Google Sheets являются источником актуальных вариантов выбора, но не источником истории: отправленные записи должны хранить выбранное значение в payload, чтобы удалённые из справочника люди и значения оставались доступны в старой аналитике.

## Последовательность реализации

### 1. Базовая серверная модель аккаунтов и данных

Сначала зафиксировать минимальные типы серверных контрактов: пользователь, тип аккаунта, platform/organization scope, выданные сервером права, показатель, период, статус ввода данных, статус подтверждения, событие аудита и канал интеграции. Отдельной runtime-сущности бизнеса и tenant ID нет.

Реализованная часть шага: четыре типа аккаунта `admin`, `business_owner`, `worker`, `dispatcher`; серверный ответ access/profile; контракты user profile, data entry status, confirmation status, dispatcher form definitions, dispatcher submissions, dispatcher feed summary и audit event summary. Администратор имеет platform scope, остальные аккаунты — organization scope; multi-business и подразделения удалены как неиспользуемые сущности. Клиент не хранит локальную матрицу прав как источник истины и не содержит прикладные фикстуры.

Результат: типизированная основа, на которую можно опирать UI и backend-контракты без смешивания данных с визуалом. Клиентская boundary для `access/profile` уже существует, а первый backend-контур реализован для dispatcher submissions; остальные backend-домены ещё впереди.

Проверки: typecheck; unit-тесты добавлять тогда, когда появятся функции правил доступа, валидации или переходов статусов.

### 2. Каркас приложения и навигации

Собрать первый реальный экран без старого визуального прототипа: запрос текущего access/profile с сервера, отображение типа аккаунта, переключение доступных разделов и пустые рабочие области там, где сервер ещё не отдаёт данные.

Первый shell уже собран как визуальная основа без production backend: профиль и доступы запрашиваются через `access/profile` boundary. В локальном Vite dev server endpoint отвечает профилем выбранной dev-сессии или `{"profile": null}`, а при отсутствующей `/api`-ручке static/frontend-хостинга вход может временно собрать клиентскую тестовую dev-сессию в `sessionStorage`. После входа пользователь может выйти из временного dev-аккаунта через общую навигацию и вернуться к выбору типа доступа; desktop rail можно свернуть до логотипа, а mobile drawer открывается из верхней панели и доступно закрывается без смены страницы. Рабочие области не показывают декоративные placeholder-плашки вместо отсутствующих серверных данных.

Результат: пользователь видит структуру продукта, но без фальшивой аналитики и без приватных данных.

Проверки: typecheck, build, ручной запуск через Vite.

### 3. Матрица ролей и доступов

Описать capabilities для серверного профиля доступа: управление пользователями, управление интеграциями, просмотр аудита, просмотр логов, использование debug-функций, просмотр статистики организации, отправка форм, отправка диспетчерских форм, просмотр диспетчерской истории, просмотр уведомлений и просмотр собственных отправок.

Реализованная часть шага: capability-значения централизованы в `src/contracts/accounts.ts`, `access/profile` отклоняет неизвестные capability, локальный Vite server выдаёт dev-профили с разными capabilities для admin, business owner, worker и dispatcher, а admin-профиль показывает только реальные capabilities из server profile без заглушечных административных панелей.

Результат: интерфейс получает права от сервера и использует их только для отображения доступных действий; сервер остаётся единственным источником защиты.

Проверки: unit-тесты на позитивные, запрещённые и пограничные сценарии доступа.

### 4. Область данных организации

Использовать одну серверную границу данных организации без tenant-иерархии: администратор работает в platform scope, любой неадминистративный аккаунт — в organization scope текущего deployment.

Результат: должность меняет набор рабочих вкладок и прав, но не создаёт tenant-сущности или технические подразделения и не разрывает общую историю организации.

Проверки: миграция старых scopes, создание аккаунтов всех типов, смена должности, typecheck, build.

### 5. Ввод данных пользователями

Сделать поток ввода данных для работника: выбор доступной формы или рабочего контекста, периода, показателя, натурального объёма, денежного значения, комментария и источника.

Текущее состояние: рабочая форма для роли `worker` ещё не реализована, и worker не получает список диспетчерских форм. Диспетчер сначала выбирает одну из 6 активных server-owned форм в 4 визуальных группах: производство, оборудование, открытие/закрытие инцидента и вход/выход посетителя. `Выработка` содержит дату отчёта, четыре одинаковые горизонтальные таблицы с динамическими марочными столбцами, три банки с замерами начала/конца дня и участок грануляции с фракциями `16/30` и `12/18`. Все поля марки диспетчера и ОЦ используют один поисковый список из `Номенклатура!A:A`; новая метка сохраняется в первую свободную ячейку сверху, а повтор марки внутри одной категории отклоняется. Во всех четырёх категориях факт и марка образуют обязательную пару: условные required-поля сразу направляют пользователя к пропущенной половине, а backend повторяет ту же проверку перед сохранением. Диспетчер вводит только `Факт за сутки`; дневной план, сумма суточных планов до выбранной даты включительно и сохранённый месячный факт предыдущих дней приходят с backend-а, после чего форма read-only показывает live-месячный факт и отклонение с текущим черновиком. Диспетчерская отправка идёт через remote boundary `VITE_SMB_REMOTE_API_URL` в backend workspace `server/`, где сервер валидирует `formId`, разрешённые статические и динамические поля `payload`, даты, числа, актуальную номенклатуру марок и сценарные ограничения. Остальные формы сохраняют ранее описанные server-owned правила оборудования, инцидентов и посетителей. Газовые формы остаются legacy-id только для чтения истории. При недоступном backend Vite dev-режим может использовать явно помеченный локальный `localStorage` fallback; он не является production-хранением и не заменяет Google Sheets или серверную валидацию.

Реализован отдельный ввод огнеупорного цеха: новая вкладка не закреплена за фиксированной должностью и назначается администратором через обычный каталог вкладок. Внутри выбирается одна из трёх таблиц, дата начала смены и смена `1 · 08:00–20:00` или `2 · 20:00–08:00`; после полуночи ночная смена по умолчанию остаётся на предыдущей дате. Имя мастера берётся из активного профиля и не передаётся как доверенный пользовательский ввод. Порядок секций, названия полей, группировка колонок и итоговые строки максимально сохранены по рабочим бумажным бланкам, а служебные подписи даты, мастера и подписей заменены общими server-owned полями и маршрутом подтверждения.

Актуальное уточнение по подсказкам анкеты `Оборудование`: ошибки локальной проверки после кнопки `Внести данные` показываются красной подсказкой, а успешное внесение остаётся обычным статусом. Если время простоя больше 0, причина простоя обязательна; если выбрана причина простоя `Резерв`, время простоя должно быть ровно 8 часов.

Актуальное уточнение по анкете `Оборудование`: `Дата отчета` является отдельной настройкой дневного отчёта; локальные UX-черновики и позиции, внесённые кнопкой `Внести данные`, хранятся по ключу выбранной даты и оборудования, а при выборе даты UI показывает сохранённые в БД отметки именно за эту дату и не переносит статус внесённой позиции с другого дня.

Актуальное уточнение по состояниям иконок анкеты `Оборудование`: при смене `Дата отчета` клиент заново запрашивает серверную историю через фильтр `reportDate` по payload отчёта, а legacy/local данные без серверной записи поднимаются только как `черновик`, не как `внесено в отчёт`. Иконка `нет данных` имеет серую заливку и тёмно-серый текст, `черновик` — серую заливку и чёрный текст.

Актуальное уточнение по навигации диспетчерских форм: после успешной отправки выработки, дневного отчёта оборудования, регистрации/закрытия инцидента, регистрации входа посетителя или регистрации выхода посетителя интерфейс автоматически возвращается на страницу выбора форм и показывает самозакрывающееся всплывающее уведомление об отправке; при ошибке отправки или проверки заполнения пользователь остаётся на текущей форме.

Актуальное уточнение по анкете `Закрытие инцидента`: после открытия сначала показывается отдельный компактный экран незакрытых инцидентов в виде обычных кнопок; каждый инцидент занимает отдельную строку, список приходит с backend-а в виде серверного `openIncidents` и отсортирован по дате открытия от новых к старым. После выбора открываются существующие поля закрытия с уже подставленным server-owned номером и возможностью выбрать другой инцидент. После первой успешной загрузки фокус получает первая кнопка инцидента, после выбора — карточка выбранного инцидента, а последующее фоновое обновление списка не перехватывает фокус повторно. Временная ошибка фонового обновления сохраняет открытую заполненную анкету; если успешное обновление подтверждает, что выбранный инцидент уже закрыт другим пользователем, UI возвращается к актуальному списку и показывает короткое объяснение.

Актуальное уточнение по пользовательским уведомлениям: для неадминистративных пользователей тосты, ошибки и статусные подсказки должны быть короткими и без технических деталей вроде id отправки, endpoint, CORS, `/health`, `localStorage`, remote/backend/server/feed; техническая диагностика остаётся в админских инструментах, логах, тестах и инструкциях.

Результат: черновик записи создаётся в статусе ожидания подтверждения и ещё не попадает в аналитический источник.

Проверки: валидация пустых, некорректных, отрицательных и неожиданных значений; unit-тесты правил создания записи.

### 6. Подтверждение данных ответственными пользователями

Добавить очередь подтверждений: кто внёс данные, кто должен подтвердить, текущий статус, комментарии, решение и timestamp.

Реализован первый production-контур подтверждения для трёх таблиц ОЦ: capability `business.review_refractory_reports` выдаётся вместе с вкладкой диспетчера `business.dispatcher_form`, очередь обновляется каждые 5 секунд, а новая запись показывает toast, счётчик в левом rail и счётчик на отдельной кнопке `Таблицы огнеупорного цеха` в меню форм. Только эта кнопка открывает экран ожидающих решений; очередь не выводится над выбором или открытой обычной формой. Решение принимается отдельно по каждой таблице; возврат требует комментарий и скрывает кнопку подтверждения, повторное или собственное решение отклоняется backend-ом. Для отправителя ОЦ отдельный `GET /api/refractory-reports/own` возвращает только ревизии текущего server-owned account ID; глобальный polling рабочего пространства показывает toast при принятии либо возврате, включает причину возврата и обновляет открытый отчёт. После commit новой pending-ревизии backend читает строки `2–20` колонок `Адресаты Диспетчеры (емейлы)` и `Адресаты Диспетчеры (МАКС)` и отправляет краткое уведомление с видом таблицы, датой, сменой, ревизией и мастером. После commit принятой ревизии адресатам колонок `Адресаты ОЦ (емейлы)` и `Адресаты ОЦ (МАКС)` отправляется полное server-owned содержимое таблицы с датой, сменой, мастером, диспетчером и рассчитанными итогами; возврат на доработку внешнюю рассылку не запускает.

Результат: только подтверждённые записи становятся источником аналитики.

Проверки: тесты переходов статусов, запрет самоподтверждения там, где оно недопустимо, проверка истории решений.

### 7. Исправления и история

Реализовать поток корректировки уже существующих данных: причина исправления, автор, дата, старое значение, новое значение, статус исправления и связь с исходной записью.

Для таблиц ОЦ реализована append-only история ревизий: после возврата пользователь исправляет сохранённые значения и отправляет следующую ревизию, а после подтверждения начинает исправление отдельной явной кнопкой. Старые pending/rejected/approved строки и авторы решений не переписываются.

Результат: ошибки, перенос бумажных данных и последующие корректировки не затирают историю.

Проверки: unit-тесты неизменности аудита и корректных переходов статусов.

### 8. Аналитические периоды и KPI

Собрать расчётные правила для последнего дня, текущего неполного месяца, текущего неполного года и динамики по выбранному периоду. Покрыть области: работники, входящие ресурсы, выполненные действия, полученный результат, задачи и активность пользователей.

Результат: аналитика строится из подтверждённых данных и явно учитывает неполные периоды.

Проверки: unit-тесты расчётов, граничные даты, пустые периоды, неполный месяц и неполный год.

### 9. Интерфейс владельца бизнеса

Собрать dashboard summary, вложенные аналитические разделы, диспетчерскую ленту и уведомления.

Реализованная часть шага: добавлена отдельная вкладка владельца `Диспетчерская`, которая запрашивает live-историю отправленных диспетчером регистраций с backend API. Левый rail владельца является единственным переключателем между `Обзор` и `Диспетчерская`; `Обзор` показывает server-owned показатели инцидентов и лаборатории за текущий месяц/сегодня и сохраняет прежние компактные блоки оборудования, месячной выработки и посетителей из dispatcher-истории. Подробные карточки последнего открытия и закрытия инцидента в `Обзоре` заменены новой числовой сводкой. `Диспетчерская` не показывает placeholder-плашки и выводит четыре рабочих раздела. Выбор раздела занимает отдельную верхнюю строку, переключатель периода располагается следующей строкой, а поля пользовательских дат при выборе `Своё` всегда открываются отдельной строкой под кнопками, включая широкий desktop. Единый переключатель периода для всех разделов содержит `Сегодня`, `Текущий месяц`, `Текущий год` и `Своё`; текущие месяц и год ограничены сегодняшней датой, а `Своё` открывает свободные поля начала/окончания, которые по умолчанию пусты и означают всю историю. В разделе `Инциденты` отдельная кнопка `Все незакрытые` одной операцией показывает серверный список открытых инцидентов за всю доступную историю независимо от выбранного периода и от загруженных страниц ленты, не меняя период остальных разделов; его длина всегда совпадает с показателем `Не закрыто сейчас` в `Обзоре`. `Выработка` разделена на шесть кнопок-таблиц: формовка, сортировка, неформованная продукция, цех обжига шамота, замеры банок и участок грануляции. Для формовки и сортировки показаны суточные и накопительные месячные план/факт с разницей; в таблицах неформованной продукции и шамота отдельная колонка показывает только названия нормализованных марок, а суточные и месячные значения остаются в собственных колонках; банки показывают начало, конец и расход; грануляция — суточные и месячные суммы фракций `16/30` и `12/18`. Месячный итог строится по последней записи каждого дня с начала календарного месяца до даты строки, даже если начало месяца не входит в видимый пользовательский диапазон; клик по дате открывает полный набор заполненных показателей в server-owned порядке полей формы. `Оборудование` агрегируется за диапазон по названию, суммарной выработке, суммарному простою и причинам простоя с часами; клик по названию оборудования открывает окно расшифровки с дневными строками из текущего периода, чтобы владелец видел составляющие итоговых чисел. `Инциденты` объединяют открытие и закрытие, показывают статус и включают записи, которые не были закрыты к началу выбранного диапазона. `Посетители` показывают входы за тот же выбранный период с временем выхода или пометкой, что выход не отмечен. В Vite dev-режиме при недоступном backend та же dispatcher-история читается из локального тестового `localStorage` и помечает источник данных.

Результат: владелец бизнеса получает обзор своего бизнеса, динамику и проблемные зоны без доступа к платформенным административным возможностям.

Проверки: typecheck, build, ручная проверка основных сценариев.

### 10. Расширенный рабочий интерфейс

Собрать формы ввода и корректировки данных, доступную статистику по серверно выданным правам и напоминания о событиях.

Реализованная часть шага: должность `Экономист` получила отдельную вкладку `План выработки` со свободным выбором месяца и категории, отдельным календарём каждой категории, пресетами всех календарных дней и понедельника–пятницы, ручной корректировкой дат и независимым сохранением категории в append-only месячный снимок. После каждого сохранения нижняя серверная сводка обновляется сразу и показывает, сколько из четырёх расписаний уже заполнено. Эта capability не выдаётся автоматически; администратор может назначить вкладку любой неадминистративной должности.

Результат: работник или пользователь с расширенными рабочими правами может поддерживать данные своего направления и видеть только разрешённую аналитику.

Проверки: тесты валидации форм и прав доступа, typecheck, build.

### 11. Ограниченный интерфейс работника

Ограничить worker-flow разрешённым вводом данных и минимальной навигацией без административных и аналитических возможностей сверх доступа.

Результат: работник может выполнить только разрешённые действия.

Проверки: тесты запрета лишних capabilities.

### 12. Интерфейс администратора

Добавить управление пользователями, типами аккаунтов, должностями, базой аккаунтов и аналитической базой. Отдельно предусмотреть смену паролей при ротации сотрудников.

Реализованная часть шага: администратор управляет учётными записями и отдельным server-backed каталогом должностей. У должности задаются название и компактный список вкладок с пояснениями; поля базового кабинета нет. Переключатель `Админ` раскрывает административные панели, но менять их и назначать содержащую их должность может только канонический аккаунт с логином `admin`; для остальных управляющих доступом эти флажки неактивны, существующий административный поднабор сохраняется, а backend независимо блокирует изменение или распространение привилегий. В таблице `Должности и доступы` администратор кнопками `Выше` / `Ниже` меняет единую последовательность: серия перемещений объединяется и автоматически сохраняется через 5 секунд после последнего действия, а до отправки ожидающий порядок можно отменить. Миграция `028_account_position_order` фиксирует текущую последовательность, новые должности добавляются в конец, а backend применяет сохранённый порядок к каталогу, группам аккаунтов, вариантам выбора должности и preview/dev-спискам; внутри должности аккаунты сортируются по имени. Аккаунт выбирает должность и наследует её название, навигацию и capabilities без индивидуальных галочек, а изменение должности отзывает сессии всех связанных пользователей. Единый workspace переключается между выданными рабочими и административными панелями независимо от технического `accountType`; системная административная должность остаётся защищённой. `Просмотр аккаунта` показывает полный каталог должностей и актуальные server-backed аккаунты, переиспользуя реальные рабочие страницы и исключая из preview реальные административные панели; `admin.account_preview` отдельно разрешает backend-у только GET рабочих данных и не выдаёт прав на их изменение, а карточки без рабочих вкладок не запускают preview до production impersonation/audit-модели. `Учётные записи` также поддерживает создание логина, сброс пароля и временное отключение входа. Миграция `035_protected_admin_accounts` добавляет server-owned защиту критичных учётных записей и автоматически защищает исходный `admin`: только он видит активное управление отметкой, а для остальных администраторов backend и UI запрещают сброс пароля, смену должности, изменение через общий редактор БД, отключение/включение входа и архивирование защищённой записи. Доступы и должности меняются только в профильных admin-экранах, а server-owned `БД` показывает только allowlisted прикладные разделы без auth-секретов, raw payload, технических ID и массового удаления. Импорт Google Sheets остаётся отдельным server-gated двухшаговым действием без перезаписи существующих записей и без email/MAX-уведомлений.

Вкладка `Действия пользователей` показывает понятный журнал за последние три календарных месяца: фильтр по каждому аккаунту отдельно, категории действий, время, имя/должность/логин автора и раскрываемые введённые значения. Просмотры фиксируют открытие основных вкладок и диспетчерских форм, но не каждый клик. Для администратора доступ проверяется backend-ом через `platform.view_audit`; у любой неадминистративной должности вкладка включается отдельно и получает `business.view_user_actions`, а backend возвращает только аккаунты и события organization scope. Старые audit-события остаются в БД и только не попадают в текущую выборку.

Результат: администратор может обслуживать структуру платформы без прямого вмешательства в аналитику вне разрешённых действий.

Проверки: тесты административных прав, запретов для неадминистраторов и валидации критичных действий.

### 13. Диспетчерская лента и уведомления

Собрать единый поток событий: новые записи, ожидание подтверждения, отклонения, исправления, напоминания, просрочки и интеграционные ошибки.

Реализованная часть шага: для диспетчерских отправок добавлен первый live-feed contract `GET /api/dispatcher/submissions` через remote API. Endpoint поддерживает фильтры `formId`, `dateFrom`, `dateTo`, постраничные `limit`/`offset` и возвращает `summary` по количеству регистраций; вкладка владельца автоматически дочитывает все страницы истории. После успешного `POST /api/dispatcher/submissions` backend отправляет email- и MAX-уведомления по открытию/закрытию инцидента и входу/выходу посетителя в человекочитаемом формате, а оборудование рассылает только после полного batch-сохранения через `POST /api/dispatcher/equipment-report` одним кратким сообщением на все позиции дневного отчёта. Для закрытия инцидента сервер дополнительно подтягивает из сохранённого открытия дату/время, место, тип, критичность и описание: уведомление после номера показывает эти исходные данные, затем введённые при закрытии корневые причины и предотвращающие меры, время закрытия и утвердившего. Тестовая среда сохраняет тот же формат и добавляет в конце отдельное примечание `Тестовое сообщение`. Общие адресаты берутся из строк `2–20` Google Sheets колонок email/MAX, дополнительные адресаты механики — из строк `22–25`, электрики — из строк `27–30`; посетительские адресаты берутся из отдельных колонок `Адресаты по посетителям (емейлы)` и `Адресаты по посетителям (МАКС)` строк `2–20`. Новые pending-таблицы ОЦ отправляют краткое уведомление диспетчерам строк `2–20` колонок `Адресаты Диспетчеры (емейлы)` и `Адресаты Диспетчеры (МАКС)`, а подтверждённые таблицы — полное содержимое отдельным адресатам колонок `Адресаты ОЦ (емейлы)` и `Адресаты ОЦ (МАКС)`; возвраты наружу не рассылаются. MAX поддерживает несколько ID в одной ячейке этих строк, спецгруппы добавляются по причине простоя, типу инцидента или тексту закрытия. Это ещё не единая диспетчерская лента всех событий.

Отправленный диспетчером отчёт `Выработка` после commit рассылается по email и MAX базовой группе адресатов оборудования из строк `2–20`; механические и электрические дополнительные группы для него не применяются. В тексте используются русские названия статуса и всех полей формы, включая динамические марки и факты четырёх категорий; техническая строка времени получения и внутренние имена полей не выводятся.

Email- и MAX-уведомление по оборудованию оформлено как нумерованный отчёт в порядке оборудования из анкеты: заголовок и дата вынесены отдельно, выработка, причина простоя, время и примечание показываются отдельными строками, нулевые показатели скрываются, а блоки оборудования разделяются пустой строкой.

Результат: владелец бизнеса, администратор и ответственные работники видят разрешённые рабочие события в одном месте.

Проверки: тесты фильтрации событий по роли, организации и статусу.

### 14. Контракты интеграций

Подготовить adapter-контракты для 1C, Excel, Google Sheets, Email и MAX без реальных секретов и SDK. Для Excel и Google Sheets отдельно описать настройку форматирования импорта.

Реализованная часть шага: первый Google Sheets reference-source добавлен без SDK и без хранения секретов в git. Backend умеет читать публичный CSV или закрытую таблицу через service account JSON на сервере, ищет значения под колонками `Места (цех/участок)` и `Ответственный за регистрацию` с двоеточием или без него, по умолчанию перечитывает результат при каждом запросе форм и обогащает серверное описание формы `Открытие инцидента`. Этот же источник читает email-адресатов и MAX ID уведомлений из фиксированных строк, включая колонки подтверждённых таблиц ОЦ `Адресаты ОЦ (емейлы)`/`Адресаты ОЦ (МАКС)` и колонки pending-уведомлений `Адресаты Диспетчеры (емейлы)`/`Адресаты Диспетчеры (МАКС)`. Для исторической миграции добавлен отдельный workbook reader exact-вкладок и import-specific parser: он читает неформатированные Google serial даты, сохраняет московское историческое время, строит lifecycle-записи инцидентов/посетителей и выдаёт предупреждения по legacy-расхождениям до подтверждения. Email-рассылка реализована через SMTP-конфиг, MAX-рассылка — через bot token и HTTP API; для Jino можно указать `MAX_CA_CERT_FILE` с доверенным PEM сертификатом Минцифры без отключения TLS, секреты хранятся только в `server/.env` и не коммитятся в git.

Результат: интеграции имеют безопасные границы и могут добавляться по одной без переписывания доменной логики.

Проверки: тесты нормализации входных данных, ошибок импорта и отсутствия логирования приватных payload.

### 15. Backend и постоянное хранение

После стабилизации frontend-прототипа выбрать backend, схему хранения, auth, audit storage и API-контракты. Frontend-only проверки доступа не считать достаточной защитой.

Текущее состояние: backend workspace `server/` хранит пользователей, password credentials, account access, каталог должностей с единым server-owned порядком, сессии, dispatcher submissions и append-only `user_audit_events` в MariaDB/MySQL. Admin API создаёт отдельные login identity/access, отклоняет повторный login, сбрасывает пароль, создаёт, редактирует и атомарно переупорядочивает должности; авторизация читает navigation items и capabilities из назначенной должности. Должности используют единый каталог `business.*` и `admin.*`, а технический `accountType` сохраняется только как внутренняя совместимость и не принимается API настройки должности. Выдача, снятие и распространение административных вкладок server-gated по каноническому login `admin`; этот же исходный администратор один управляет server-owned защитой аккаунтов, а защищённые записи атомарно закрыты от сброса пароля, изменения, отключения и архивирования всеми делегированными администраторами. `admin.account_preview` разрешает только чтение каталогов аккаунтов/должностей и GET рабочих данных без производных изменяющих `business.*` capabilities. `/api/admin/access-levels` удалён. Миграция `009_remove_account_access_levels` удалила старые presets, `010_dynamic_account_positions` ввела текущий каталог должностей, `014_dispatcher_spreadsheet_import_source` добавила уникальный source key для повторяемого импорта, `015_user_audit_events` создала постоянный журнал, `016_remove_departments` удалила модель подразделений, `028_account_position_order` добавила постоянный порядок должностей, а `035_protected_admin_accounts` — защиту критичных аккаунтов с backfill исходного `admin`. Миграция `017_single_organization_scope` сохранила историю, перевела все неадминистративные доступы в organization scope, удалила business ID из рабочих таблиц и затем удалила таблицу бизнесов. Audit API серверно фиксирует входы/выходы, отправки форм, изменения БД, импорт, управление аккаунтами/должностями и allowlisted просмотры экранов; production-изменения и их audit-события проходят через общий транзакционный DB-контекст, а `GET /api/admin/audit-events` жёстко применяет rolling-окно трёх календарных месяцев и не удаляет более старые записи. Для администратора endpoint возвращает платформенный отчёт, а для неадминистративной должности с `business.view_user_actions` сервер ограничивает события и каталог аккаунтов organization scope. Database API отдаёт только безопасные редактируемые пользовательские представления; backend формирует поля редактора, валидирует статусы и диспетчерские формы, пересчитывает производные значения и не передаёт auth secrets, session identifiers, raw payload, dedupe/source keys и неизвестные таблицы. Import API отдельно валидирует прикладные данные, схлопывает одинаковые строки по нормализованному содержимому и транзакционно добавляет только отсутствующие dispatcher submissions единой организации, используя уникальный organization-wide `dedupe_key` как защиту от гонок. Лимит server/client dispatcher history увеличен до 2000 записей, чтобы первичный импорт 572 строк не вытеснил incident/visitor lifecycle из рабочих представлений. Test/production разделены через `VITE_SMB_APP_ENV`, `SMB_APP_ENV`, `DEV_ACCESS_ENABLED`, отдельные cookie/env/БД; production использует login/password auth и запрещает dev-session/client fallback. Personal plugin и VS Code extension `smb-jino-deploy` запускают существующий tracked deploy по SSH, не хранят секреты, требуют SSH-ключ, показывают актуальность test/production по post-publish marker, отображают один стандартный progress bar по реальным этапам deploy без автоматического открытия Output-панели и после публикации проверяют HTTP redirect и HTTPS health обеих сред; неинтерактивный SSH-runner отдельно активирует системный Jino Node.js 22 runtime из `/opt/alt` и проверяет Node/npm ещё в read-only preflight, а optional remote tests выполняются через последовательный `test:jino`, чтобы не превышать лимит процессов/потоков managed hosting. Production impersonation, безопасный bootstrap reset с re-auth/backup guard и общая аналитическая БД ещё не реализованы.

Миграция `009_remove_account_access_levels` завершает переход к полностью индивидуальным настройкам: она не меняет сохранённые `navigation_items` и capabilities аккаунтов, удаляет все оставшиеся связи и затем удаляет колонку `access_level_id` вместе с таблицей пресетов. Endpoint `/api/admin/access-levels` и связанные frontend/backend-контракты удалены.

Результат: появляется реальная доверенная граница для данных, ролей, подтверждений, истории и интеграций.

Проверки: тесты API-контрактов, авторизации, валидации входа, миграций и audit trail.

### 16. Финальная полировка MVP

Проверить производительность ключевых экранов, пустые состояния, ошибки, адаптивность, доступность, безопасность логов и отсутствие приватных тестовых данных.

Результат: MVP готов к демонстрации и дальнейшей интеграционной разработке.

Проверки: typecheck, build, релевантные unit/integration-тесты, ручной smoke-test основных ролей.

## Ближайший следующий шаг

Текущий активный пункт: получить текст Положения и сверить с ним поля формы, затем согласованно выпустить backend и миграции `022_google_sheets_production_brands`–`035_protected_admin_accounts` в test/production. Отдельно проверить перестроенный расчёт насыпного веса банок по последним записям `Журнала печи 2`. После deploy через штатный admin-flow проверить сохранение порядка должностей и его применение к аккаунтам, убедиться, что только login `admin` может включать `admin.*`, назначать смешанные должности и менять защиту аккаунтов; пометить тестовый административный аккаунт защищённым и подтвердить отказ делегированному администратору в сбросе пароля, смене должности, изменении через БД, отключении и удалении. Также проверить, что у делегированного менеджера административные флажки отключены и существующий поднабор сохраняется, выбрать Ларикову А.Т., Самсонову Г.Н. и Глушкову Ю.В. вариант доступа с созданием, приёмкой и возвратом на доработку и проверить общий поиск марок, живые вкладки `Лаборатория`, `Банки` и группу `ЦЗЛ` с журналами `Журнал печи 2`, `Регистрация проб` и `Химические анализы`, ввод/фильтрацию трёх лабораторных журналов, средний насыпной вес, выбор зарегистрированной пробы и подтягивание последнего химического анализа в историю регистрации, загрузку/удаление до пяти PDF в поручении и защищённый протокол №369, а также полный путь поручения для четырёх вариантов доступа: создание расписания с документами → редактирование живой версии другим постановщиком → появление активной даты у исполнителя → передача на проверку → возврат на доработку или завершение проверяющим → неизменяемый снимок с документами в истории → переход к следующему повтору. Отдельно подтвердить, что комментарии отображаются в хронологическом формате, удалённый из живой версии PDF открывается из ранее завершённого снимка, завершённый снимок не меняется после последующей правки повторяющегося поручения и отмена закрывает карточку без записи. После этого продолжить настройку Jino test read-only `PRODUCTION_DATABASE_URL`, синхронизацию production → test, проверку должности `Экономист`, независимых планов категорий и production-импорта книги `Диспетчеры`.

К указанному диапазону deploy добавить миграции `036_sample_registration_sampling_location_index` и `037_sample_registration_water_absorption`. В smoke-проверку `ЦЗЛ → Регистрация проб` включить выбор стандартного места отбора, ввод нового места с повторным появлением в раскрывающемся списке после успешного сохранения и обязательное сохранение водопоглощения; старая запись без нового показателя должна по-прежнему открываться с `—` в соответствующей колонке.

Следующий шаг: создать production-пользователя БД только для чтения, внести три snapshot-переменные в непубличный `server/.env` test, выполнить `SMB_DEPLOY_BRANCH=main npm run deploy:jino:dual`, проверить одинаковые миграции обеих сред и запустить синхронизацию из test-вкладки `БД`; убедиться, что production не изменилась, test полностью совпала по данным, а прежние auth-сессии требуют повторного входа. Затем выполнить production preview/commit импорта и smoke-проверку остальных рабочих кабинетов.
